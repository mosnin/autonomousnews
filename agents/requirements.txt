# Pinned dependencies for the Techno Times agent worker.
# Regenerate with:
#   pip-compile --output-file=requirements.txt pyproject.toml
# (or `uv pip compile pyproject.toml -o requirements.txt`)

modal==0.66.41
openai==1.54.0
openai-agents==0.0.5
httpx==0.27.2
pydantic==2.9.2
